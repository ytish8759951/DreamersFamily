# DreamersFamily Full-Site Cross-Device Sync Matrix

Last audited: 2026-07-23 Asia/Taipei

## Product Rule

Any data that a parent or child has explicitly saved/submitted is formal business data and must use Supabase as the source of truth. IndexedDB and localStorage are allowed only for unsent drafts, media previews, offline cache, session/device binding, and UI state.

## Shared Sync Layer

| Area | Current implementation | Audit result |
| --- | --- | --- |
| Initial snapshot | `SupabaseDataRepository.fetchSupabaseState()` for parent sessions; `fetchChildScopedSupabaseState()`/`get_child_scoped_repository_snapshot` for child sessions. | Formal snapshot exists. Parent snapshot now includes `badges` and `child_badges`; this was missing before this audit. |
| Realtime | `subscribeToSupabaseChanges()` and `subscribeToChildScopedSupabaseChanges()`. | Parent now subscribes to `task_records`, `badges`, and `child_badges` in addition to the existing shared tables. Child subscriptions are child-scoped. |
| Recovery refetch | Repository constructor listens to `online`, `focus`, and `visibilitychange`, then calls `hydrateFromSupabase()`. | Present and shared. |
| Merge key | Shared helpers merge by formal `id`; task merge also uses formal task records. | Correct rule. Do not dedupe by title/photo/date. |
| Formal writes | Several flows use dedicated RPCs, but many still call `delegateWrite()` and push cached state to normalized tables. | Needs hardening: cache-first writes are acceptable as offline queue only when the later Supabase write is idempotent and scoped. Money/time/purchase writes need more dedicated transaction RPCs. |
| Media | `mediaRepository` and share media save Storage path/media id; signed URLs are generated at read time. | Correct rule for current share/growth/task media. Legacy IndexedDB-only media must be backfilled from the original device; fake replacement media is not allowed. |

## Page Matrix

| Page / feature | Parent writes | Child writes | Formal tables / RPC | Storage / media reference | Realtime | Refetch | Idempotency | Scope | Offline recovery | Test result |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 任務管理: add/edit/delete/assign | `taskRepository.createTask()` through `upsert_parent_task_from_repository`; existing edit/delete paths still pass through repository state. | Child complete uses child-scoped repository delta; parent approval uses RPC. | `tasks`, `task_records`, `stars`; RPC `upsert_parent_task_from_repository`, `ensure_daily_task_instances`, `get_child_scoped_repository_snapshot`, `sync_child_scoped_repository_delta`, `approve_task_with_stars`. | Task image ids: `image_media_id` / `thumbnail_media_id` with `media_assets` and `family-media`. | Parent/child: `tasks`, `task_records`, `stars`; parent was missing `task_records` and is fixed in this audit. | `focus`, `visibilitychange`, `online`, reload. | Create uses `client_request_id`; task approval star ledger uses `task:${task.id}:stars`; daily instances unique by template/child/date. | Parent family scoped; child session child scoped. | Pending local cache can retry; formal rows win by id. | Passed existing local/task regression; needs live two-device smoke after deployment. |
| 每日任務 | Parent creates daily template and today's instance by RPC. | Child reads only today's Taipei-date active instances. | Migration 031/032 RPCs and constraints. | Template media copied to instance snapshot. | Same as tasks. | Same as tasks. | Unique daily template + child + `occurrence_date`; RPC uses conflict-safe insert. | Same family/child limits. | Old unfinished daily instances expire, not delete. | Regression covered by `taskSyncRegression.test.ts` and WebKit task flow. |
| 今日分享: photos/audio/video | Parent reads, deletes/reviews, encourages. | Child creates share after media upload succeeds. | `shares`, `share_media`, `media_assets`, `stars`; RPC `encourage_share_with_stars`. | `family-media`; DB stores bucket/object path/mime/size/sort order, not signed URL. | Parent/child: `shares`, `share_media`, `stars`. | Same global refetch. | Share encouragement uses ledger key `share_reward:${share.id}` and DB uniqueness; share creation is still repository-id based and should be moved to a dedicated RPC for all-or-nothing media+share creation. | Family/child scoped. | Media upload must finish before share row; failed media must not create empty share. | Existing share media tests cover multi-photo/video/audio guards; needs live Supabase media smoke. |
| 撲滿 / 商品兌換 | Parent income, product CRUD, purchase status now call dedicated Supabase RPCs in Supabase mode. | Child purchase request/cancel/arrival confirmation now call the same purchase event RPC with child device binding scope when available. | `piggy_banks`, `piggy_bank_records`, `store_items`, `purchases`; RPC `create_piggy_income_with_deposit`, `upsert_piggy_product_from_repository`, `apply_piggy_purchase_event` in migration 033. | Product rows now persist `main_media_id`, `gallery_media_ids`, and Storage media ownership through `media_assets` / `family-media`; payload snapshot remains compatibility data, not the only reference. | Parent/child: `piggy_bank_records`, `store_items`, `purchases`. | Same global refetch. | `client_request_id` unique indexes on piggy records, products, and purchases; purchase debit/refund is guarded by RPC balance and request uniqueness. | Parent family scoped or verified child device binding scoped. | Cache retry remains fallback; formal RPC rows are source of truth and merge by formal id. | `npm test` and production build passed after migration 033 code changes; WebKit/live Supabase still required before deployment. |
| 平板時間 | Parent add/deduct/use ledger writes now call `apply_tablet_time_log` in Supabase mode; planned schedules and request review still use existing repository path. | Child use-time writes call the same RPC with child device binding scope when available; star redemption still needs a combined `stars` + `tablet_time` transaction RPC. | `tablet_time`; RPC `apply_tablet_time_log` in migration 033; existing stars ledger remains separate for redemption. | No media. | Parent/child: `tablet_time`. | Same global refetch. | `client_request_id` unique index on `tablet_time`; RPC prevents negative balance and duplicate add/deduct/use logs. | Parent family scoped or verified child device binding scoped. | Cache retry remains fallback; formal rows merge by id. | `npm test` passed after ledger RPC changes; star redemption transaction hardening remains. |
| 成長紀錄 | Parent can create/edit/delete. | Child can create where UI allows. | `growth_records`; repository state for memory packs/annual notes in parent settings. | Photos through `media_assets` / `family-media`; no blob URL as final URL. | Parent/child: `growth_records`. | Same global refetch. | Record ids only; create/update/delete needs stronger request id if rapid retry matters. | Family/child scoped. | Existing cache can retry; formal records merged by id. | Local growth tests pass; live media smoke still required. |
| 愛的信箱 | Parent send/read updates now call `upsert_mailbox_message_from_repository`; child read/reply paths use the same repository method and verified child device scope when present. | Child message/read state writes use the same RPC where UI supports child-origin messages. | `encouragement_cards`, `media_assets`; RPC `upsert_mailbox_message_from_repository` in migration 033. | Attachments persist `media_id`, `media_bucket`, `media_path`, and MIME; Storage objects stay in `family-media` and are resolved by signed URL through `mediaRepository`. | Parent/child: `encouragement_cards`. | Same global refetch. | `client_request_id` unique index on mailbox messages; read/update upserts by formal id. | Parent family scoped or verified child device binding scoped. | Cache retry remains fallback; formal rows merge by id. | `npm test` passed after mailbox RPC changes; live attachment smoke still required before deployment. |
| 特別的日子 | Parent add/edit/delete. | Child reads; child write only if UI allows. | `special_days`. | Photos should use `media_assets` / `family-media` if attached. | Parent/child: `special_days`. | Same global refetch. | Row ids only. | Family scoped, optional child id. | Cache retry exists. | Local special day child filter test passes. |
| 榮譽牆 / 徽章 | Parent creates/deletes badge catalog through `upsert_badge_catalog_from_repository`; parent awards through `award_child_badge_from_repository`. | Child reads own awards from child scoped snapshot and Realtime. | `badges`, `child_badges`, `stars`; RPC `upsert_badge_catalog_from_repository`, `award_child_badge_from_repository` in migration 033. | Badge catalog has `image_media_id`; current UI is icon based, future image badges should use `media_assets` / `family-media`. | Parent: `badges`, `child_badges`, `stars`; child: `child_badges`, `stars`. | Same global refetch. | `client_request_id` unique indexes on badge catalog and child awards; child+badge unique guard; award RPC writes badge stars once to the existing stars ledger. | Parent family scoped; child award rows child scoped. | Cache retry remains fallback; formal rows merge by id. | `npm test` passed after badge RPC changes; live Supabase smoke still required before deployment. |
| 孩子個人資料 / 我的家 | Parent edits child name/avatar/family settings. | Child reads own profile and device binding state. | `children`, `device_bindings`, `parents.settings.family_settings`; child login RPCs. | Avatar/media should use `media_assets` if file based. | Parent/child: `children`, `device_bindings`; parent settings are hydrated from `parents`. | Same global refetch. | Child token/device binding RPCs are idempotent by device binding constraints. | Family/child scoped; child session child id is authoritative. | Session stays device-local; binding formal DB. | Child session tests pass. |
| 家長設定 | Parent-only UI settings. | Not applicable except child-facing settings. | Parent-only UI may remain local; child-facing config goes to `parents.settings.family_settings`. | Media only if settings include file refs. | Parent settings update through parent snapshot hydration; no normalized family settings table. | Same global refetch. | Row id is parent id. | Parent/family scoped. | Local UI prefs may stay local. | Settings cleanup safety tests pass. |

## Root Causes Found

1. `task_records`, `badges`, and `child_badges` were not fully represented in the parent Realtime/fetch path, so some cross-device updates depended on unrelated table changes or stale repository snapshots.
2. Several shared features still use a cache-first `delegateWrite()` pathway. This is not by itself wrong for offline support, but formal business operations that change balances or states must be promoted to scoped RPCs with idempotency.
3. Media flows are mostly formalized for shares and existing media repositories, but old IndexedDB-only media can only be repaired from the original device that still has the Blob.

## Follow-Up Required Before Claiming Full Transactional Coverage

The following are product-critical hardening items that still need dedicated RPCs or explicit product decisions:

1. Screen-time star redemption and request approval should write `stars` and `tablet_time` in one transaction RPC.
2. Delete operations that currently depend on cache snapshots must be audited so stale devices cannot remove formal rows that another device created.

# DreamersFamily Full-Site Cross-Device Sync Matrix

Last verified: 2026-07-23 Asia/Taipei

## Product Rule

Any data that a parent or child explicitly saves or submits is formal business data. Supabase snapshot/RPC rows are the source of truth. IndexedDB and localStorage are only for unsent drafts, media previews, offline retry cache, session/device binding, and UI-only preferences.

Full-site sync must not be claimed complete without Production Live E2E evidence; this document records that evidence.

## Shared Sync Layer

| Area | Current implementation | Final status |
| --- | --- | --- |
| Initial snapshot | Parent uses `SupabaseDataRepository.fetchSupabaseState()`. Child sessions use child-scoped snapshot/RPC. | PASS. Parent and child snapshots are scoped by `family_id`/`child_id`. |
| Realtime | Parent and child repository subscriptions cover shared formal tables including tasks, shares, media-driven rows, badges, stars, tablet time, piggy bank, mailbox, growth records, and special days. | PASS. Recovery refetch also runs on `online`, `focus`, and `visibilitychange`. |
| Formal writes | Migrations 034-038 harden dedicated RPCs for share create/delete, screen-time redemption request/review, growth CRUD, special-day CRUD, mailbox read/update, and related idempotency. | PASS. Production Live E2E evidence is in `artifacts/live-e2e/E2E-20260723042018.json`. |
| Delete/cache | Formal tombstones (`deleted_at` / `archived_at`) and authoritative snapshots drive visible lists. Local cache is not allowed to resurrect deleted formal rows. | PASS for the tested shared flows. Cleanup of E2E data used only product cleanup RPC. |
| Media | Formal media uses `media_assets` plus private `family-media` Storage paths. Signed URLs are generated at read time. | PASS. Product/share/growth/special-day/mailbox/task media signed URLs returned HTTP 200 with expected bytes; audio/video signed URLs loaded metadata in browser. |

## Page Matrix

| Feature | Parent writes | Child writes | Formal tables / RPC | Storage / media reference | Realtime | Refetch | Idempotency | Scope | Offline recovery | Test result |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 任務管理 | Parent creates and approves tasks through formal task RPCs. | Child reads child-scoped visible tasks; completion uses child-scoped repository delta. | `tasks`, `task_records`, `stars`; RPC `upsert_parent_task_from_repository`, `ensure_daily_task_instances`, `sync_child_scoped_repository_delta`, `approve_task_with_stars`. | Task image uses `thumbnail_media_id` / `media_assets` / `family-media`. | `tasks`, `task_records`, `stars`. | `online`, `focus`, `visibilitychange`, reload. | Create uses `client_request_id`; approval stars use one ledger key. | Parent family scoped; child child scoped. | Formal rows win by id; archived rows are filtered. | PASS: task `99a990b3-bbd8-45ca-98a4-95600f4ced9c`, visible child instance `6d6cb2f2-d45b-4838-b670-a567dc3f629f`, stars 18 -> 21, visible duplicate count 1. |
| 每日任務 | Parent creates daily template and instance through RPC. | Child sees one visible occurrence for today. | Same task RPCs plus daily instance generation. | Template media copied to instance snapshot. | Same as tasks. | Same as tasks. | Daily instances unique by template/child/date. | Parent family scoped; child child scoped. | Old/inactive rows are filtered by child snapshot. | PASS: parent formal rows show one template and one occurrence; child visible count is 1. |
| 撲滿 / 商品兌換 | Parent creates income/product and confirms purchase lifecycle through RPCs. | Child purchase event uses verified child binding RPC. | `piggy_banks`, `piggy_bank_records`, `store_items`, `purchases`; RPC `create_piggy_income_with_deposit`, `upsert_piggy_product_from_repository`, `apply_piggy_purchase_event`. | Product media `346c87b1-56e8-4876-b2ca-226ba2019a54`, Storage 70 bytes, signed URL HTTP 200. | `piggy_bank_records`, `store_items`, `purchases`. | Same shared refetch. | `client_request_id` guards income/product/purchase debit. | Parent family scoped; child binding scoped. | Formal balance derives from Supabase rows. | PASS: income `5d215a83-2400-4d65-912d-c5ced626b08e`, deposit `82c4f432-656a-4993-8b9f-be88e0a05904`, balance 0 -> 100; product `5aa1bab7-fdc5-4b54-9c2e-9d4fbae5a054`; purchase `3d1e0c55-8f09-47c5-af79-fd64d21ae379`, debit `bca51cd0-9b41-437e-af02-ad3e1ecbffa1`, balance 100 -> 70, retry debit count 1. |
| 平板時間 | Parent approves redemption through one transaction RPC. | Child creates redemption request through verified binding RPC. | `tablet_time`, `stars`; RPC `create_screen_time_redemption_request`, `review_screen_time_redemption_request`, `apply_tablet_time_log`. | No media. | `tablet_time`, `stars`. | Same shared refetch. | Request/log rows use `client_request_id`; migration 038 derives a distinct log key if clients reuse the request key. | Parent family scoped; child binding scoped. | RPC rejects negative stars/time. | PASS: redemption `fbabc507-9aa1-482a-b2a8-b3f9fa51484b`, stars ledger `1366248c-16e0-4288-900c-fa39cd45e645`, tablet log `846c9f74-21ef-42cc-968d-ae97e3f7a6d4`; stars 20 -> 18, minutes 0 -> 10. |
| 今日分享 | Parent reads, deletes, reviews, and encourages shares. | Child uploads media then finalizes share with dedicated RPC. | `shares`, `share_media`, `media_assets`, `stars`; RPC `create_share_from_repository`, `delete_share_from_repository`, `encourage_share_with_stars`. | Multi-photo media `e8956e69-5401-4614-acdf-2749778799bf`, `1f24509e-7847-41f5-afb2-468e52e25790`; audio media `e30cc234-7ddf-44f6-928a-6060b572491c`; video media `eccb3323-5c55-4ce5-84ec-5530ea1ee6bc`. | `shares`, `share_media`, `stars`. | Same shared refetch. | Share create uses unique `client_request_id`; delete tombstones. | Family/child scoped; child binding scoped. | Deleted shares are excluded from visible lists and cannot be restored by local cache. | PASS: multi-photo share `195fad97-8e57-4d84-a3f1-d844a5a590c4`, audio share `e6a32904-ac64-42ec-847a-a1cba009f3ac`, video share `d65ab9ab-c283-4636-a5b4-107e685660fb`; deleted_at `2026-07-23T04:20:30.759792+00:00`; audio 4044 bytes/duration 0.25, video 380 bytes/duration 0.001, all signed URLs HTTP 200. |
| 成長紀錄 | Parent create/update/delete calls dedicated RPC. | Child reads child-scoped snapshot; child write path uses same repository RPC when available. | `growth_records`, `media_assets`; RPC `upsert_growth_record_from_repository`. | Media `480eca05-5008-47dd-ae3b-3f1c3d0af89e`, Storage 70 bytes, signed URL HTTP 200. | `growth_records`. | Same shared refetch. | Create/update/delete carry `client_request_id`; delete uses `deleted_at`. | Family/child scoped. | Tombstones remain formal and are filtered from visible lists. | PASS: record `78e9e5b0-2e9d-49d5-a8e4-1b88564e3d44`, updated_at `2026-07-23T04:20:32.109+00:00`, deleted_at `2026-07-23T04:20:32.054747+00:00`. |
| 愛的信箱 | Parent sends message through RPC. | Child marks opened through the same RPC with binding scope. | `encouragement_cards`, `media_assets`; RPC `upsert_mailbox_message_from_repository`. | Media `4453b04c-c7b4-495c-b156-554c4569e1c4`, Storage 70 bytes, signed URL HTTP 200. | `encouragement_cards`. | Same shared refetch. | Create uses `client_request_id`; migration 036 preserves parent-owned required fields on child read update. | Parent family scoped; child binding scoped. | Formal status wins by id. | PASS: message `08843b48-28cb-428f-9b93-a5063315d8a9`, status sent -> opened. |
| 特別的日子 | Parent create/update/delete calls dedicated RPC. | Child reads child-scoped snapshot. | `special_days`, `media_assets`; RPC `upsert_special_day_from_repository`. | Media `d1751f15-bf63-46ce-ab7f-f58a4eece507`, Storage 70 bytes, signed URL HTTP 200. | `special_days`. | Same shared refetch. | Create/update/delete carry `client_request_id`; delete uses `archived_at`. | Family scoped or child scoped. | Archived rows are excluded; other children receive only own/family-scoped rows. | PASS: special day `c5f32f15-884b-4636-ace7-69a19c7e6d3e`, updated_at `2026-07-23T04:20:33.318322+00:00`, archived_at `2026-07-23T04:20:33.448761+00:00`. |
| 榮譽牆 / 徽章 | Parent creates badge catalog and awards badge through RPC. | Child reads own awards and stars. | `badges`, `child_badges`, `stars`; RPC `upsert_badge_catalog_from_repository`, `award_child_badge_from_repository`. | Current badge UI is icon based; future image badges must use `media_assets` / `family-media`. | `badges`, `child_badges`, `stars`. | Same shared refetch. | Badge and award rows carry `client_request_id`; award RPC writes stars once. | Parent family scoped; child award child scoped. | Formal awards and star ledger merge by id/key. | PASS: child_badge `575d8dd3-52e7-4f64-9977-c7887cc3e96b`, stars ledger `d14b52b2-9d43-4ea1-94ab-61cbf580cf5d`, stars 0 -> 20, retry star count 1. |
| 孩子個人資料 / 我的家 | Parent edits child profile and family settings. | Child reads own profile and binding state. | `children`, `device_bindings`, `parents.settings`; child login RPCs. | File avatars should use `media_assets` if present. | `children`, `device_bindings`. | Same shared refetch. | Device binding constraints and login RPCs are formal. | Family/child scoped. | Session tokens and device labels are device-local by design. | PASS: child binding `9068e759-8104-439a-a5e1-844589310fc0:69e278a9-1b55-428c-83a2-109baa6b4e43`. |
| 家長設定 | Parent-owned settings write to formal parent/settings rows when they affect child-facing behavior. | Child receives child-facing settings through snapshot. | `parents.settings`, `children`, related feature rows. | Media only if the setting includes file refs. | Parent/settings hydration. | Same shared refetch. | Parent row id is formal id. | Parent/family scoped. | Device-only UI preferences stay local because they are not business data. | PASS by product rule; no shared business row is left local-only. |

## Verification Status

| Check | Status |
| --- | --- |
| `npm test` | PASS after migrations 035-038: 20 files, 136 tests. |
| Production build | PASS after migrations 035-038: `tsc --noEmit && vite build`. |
| WebKit/iPad regression | PASS after migrations 035-038: 14 Playwright WebKit tests. |
| Supabase migration dry-run / push | PASS. 035, 036, 037, and 038 each dry-ran and applied to the linked production Supabase project. |
| Local/remote migration consistency | PASS: 001-038 present both local and remote. |
| Formal two-context Production Live E2E | PASS. Final report: `artifacts/live-e2e/E2E-20260723042018.json`; QA family `5f40b7e9-7ec6-48f6-b825-2bbd5efe80af`, child `9068e759-8104-439a-a5e1-844589310fc0`. |
| E2E cleanup | PASS. Final E2E family removed by `execute_test_data_cleanup`; failed-run families `f7a4b128-0b0a-4aed-85e4-ffed029820ca`, `d01f9b57-8667-4b3b-b937-fc03b15d6927`, and `b2c80c33-dc4d-4d11-83b1-baaf0e3b0234` also removed by the same product cleanup RPC. |
